{
  "rewrites": [
    { "source": "/(.*)", "destination": "/public/$1" }
  ],
  "crons": [
    {
      "path": "/api/checkArbitrage",
      "schedule": "*/1 * * * *"  // Run `checkArbitrage.js` every minute
    }
  ]
}
